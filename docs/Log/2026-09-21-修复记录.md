# 2026-09-21 修复记录

记录对象为本案例的 Windows 10 LTSC 2021 环境；详细版本和文件哈希统一见 [使用与回退](../03-使用与回退.md)。此处只保留关键结论，不作为安装指南。

## 关键尝试

| 尝试 | 结果与处置 |
| --- | --- |
| 检查原版截图与文字 | 文字读取成功；截图报 `SetIsBorderRequired / 0x80004002`，系统能力检测确认接口缺失。 |
| 备用 GDI 截图 | 整屏、单屏和裁剪成功；可用于临时取图，但不等于原生接口修复。 |
| 切换包内 Swift 后端 | 枚举成功，截图时进程退出 `0xC0000409`；完整回退。 |
| 仅跳过边框设置 | 进入后续捕获阶段，但仍超时；完整回退。 |
| WGC 转换对照 | 已收到有效帧；回调内等待转换超时，独立 MTA 转换及同步纹理读取成功。 |
| 兼容层初版 | 推进到 JPEG 编码，但 Premultiplied alpha 被拒绝；回退后改为与正常转换一致的 Straight alpha。 |
| 当前兼容层 | 官方资源管理器截图和文字读取成功；完整回退、重新安装后再次通过。 |

相关失败脚本及临时对照程序已从当前版本清理，原有五次提交保留在 Git 历史中。当前源码仅保留有效兼容层、构建、安装回退、环境诊断和备用截图工具。

## WGC 对照摘要

以下为当时同一轮测试结果，耗时随负载变化；临时程序仅用于故障定位，现已移除。

| 测试 | 结果 |
| --- | --- |
| 主屏基础帧捕获 | 172 ms 收到有效帧，3840×2160 |
| 回调内等待异步转换 | 已收到帧，等待 2 秒后超时 |
| 独立 MTA 线程转换 | 成功，约 31 ms |
| 主屏经兼容层转换 | 成功，约 31 ms，3840×2160 |
| 左侧竖屏经兼容层转换 | 成功，约 15 ms，1440×2560 |
| WARP 软件设备经兼容层转换 | 成功，约 62 ms，3840×2160 |
| 资源管理器窗口经兼容层转换 | 成功，1391×1069 |

## 官方接口与回退验收

验收通过标准 MCP 调用已配置的官方 `node_repl`，在 JavaScript 中使用 `@oai/sky`，经应用管理的原生管道到辅助程序。用户明确允许资源管理器读取和截图，未执行文件移动、删除或文字输入。

- 官方接口返回真实 JPEG 和截图 ID；已目视核对 1100×849 的窗口图，文字、图标与文件列表正常。该尺寸是官方输出的缩放图尺寸，与原始捕获尺寸不同。
- 同时请求截图与文字，连续三次成功。完整回退并重新安装后，再连续三次成功。
- 重装后的最后一组记录为 16:09:18—16:09:20（Asia/Shanghai），每次包含 5 张工具输出图，文本长度为 7637、7688、7688；输出图数量可能随附属窗口变化。
- 回退时原程序哈希恢复，厂商签名为 `Valid`，兼容 DLL 移除成功；重新安装后的辅助程序签名为 `NotSigned`。
- 从完整原版备份重新构建，辅助程序和 DLL 哈希与当前安装一致；空文件、错误内容、已修改辅助程序三种输入被版本校验拒绝。
- 备用整屏 5280×2560、主屏 3840×2160、局部 1000×700 截图成功，5 项坐标测试通过。

**已在指定版本、本机资源管理器场景验证原生截图恢复；其他版本和业务场景未据此获得验证。** 微信小程序等业务页面、HDR、截图坐标点击和完整 Codex 更新或重启不在已通过范围内。

## 清理后的保留内容

有效原版备份和构建产物保存在 Git 忽略的 `artifacts/` 中，具体路径及恢复命令见 [使用与回退](../03-使用与回退.md)。旧截图、完整捕获输出、反汇编、失败备份和重复构建已清理，公开内容只保留上述验收摘要。

文档整理没有重新安装或卸载兼容层；当前程序和回退备份通过只读哈希核对。整理后的源码仍可从原版备份构建相同产物，坐标测试通过。截图结论沿用上面的实际验收记录，不将本次文档整理当作一次新的截图测试。

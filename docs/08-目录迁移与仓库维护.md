# 08 目录迁移与仓库维护

检查日期：2026-09-21。当前项目文件夹及 Codex 项目名称均为 `fix_computer-use`。

## 路径检查结论

项目更名没有破坏 Git 仓库或脚本中的项目路径。检查范围包括版本控制中的文档、脚本、结构化证据、Git 配置，以及本机 `artifacts/` 中的 JSON 元数据和备份清单。

| 检查项 | 结果 |
| --- | --- |
| Git 根目录和 `.git` | 均位于当前项目文件夹内，`.git` 为普通目录 |
| 写死的旧项目路径 | 未发现 |
| Git 外部工作区绑定 | 未配置 `core.worktree`、`core.hooksPath` 或 `include.path` |
| 自定义 Git 钩子 | 未发现已启用的自定义钩子 |
| Git 历史完整性 | `git fsck --full` 通过，前三次提交保持不变 |
| Python 默认输出路径 | 根据 `Path(__file__).resolve()` 推导项目根目录 |
| PowerShell 输出与备份路径 | 根据 `$PSScriptRoot` 计算，不依赖项目文件夹名称 |
| 文档内部链接 | 使用相对路径，已随中文文件名更新 |
| UTF-8 与本地链接复查 | 23 个项目文件解码成功，20 处本地链接均可解析 |
| 现有坐标测试 | 5 项全部通过 |
| 更名后显示器枚举 | 两块显示器读取成功，虚拟桌面为 5280×2560 |
| 更名后备份路径 | 状态脚本确认 `BackupExists: true` |
| 已安装原生程序 | `IsOriginal: true`、`IsPatched: false`、`Signature: Valid` |

现有实验脚本仍将 Codex 的运行时子目录 `df473e5367fa2b42` 及目标文件哈希限定为上次实测版本。这是安装位置和版本保护，与项目文件夹名称无关。后续 Codex 更新若改变运行时目录，需要重新检查实际安装位置和文件版本，不能只改路径后直接执行实验补丁。

任务启动时保存的默认工作目录仍指向更名前的位置，首次命令因此报“目录名称无效”。本次操作已显式指定新的工作目录；Codex 项目列表也已确认指向新位置。该任务上下文问题不影响 Git 提交内容，不需要重建仓库或改写历史。

## 文档整理

- 原有七篇文档的文件名改为 `01-中文名称.md` 至 `07-中文名称.md`，一级标题同步保留序号。
- 原始 Markdown 和归档目录已从当前工作区删除，相关链接改为说明基线提交 `2df52f4`。
- 本次删除保留已有提交历史，因此仍可在 Git 中查阅原始资料；没有执行历史重写。
- `docs/evidence/` 下的 JSON 文件是结构化证据，保留原有机器可读文件名。

## 私有仓库

仓库：[llbk-kevin/fix_computer-use](https://github.com/llbk-kevin/fix_computer-use)。

- 可见性：Private。
- 远端名称：`origin`。
- 主分支：`main`，跟踪 `origin/main`。
- About：Windows 上 Computer Use 截图故障的诊断记录、修复试验与回退脚本，附支持多显示器的本地备用截图工具。

同步范围为 Git 管理的源代码、文档和脱敏证据。`artifacts/` 下的桌面截图、二进制备份、完整诊断输出，以及 Python 缓存和临时文件继续由 `.gitignore` 排除。

## 后续检查和同步

在当前项目文件夹打开命令提示符后执行：

```bat
git status --short --branch
git remote -v
python -m unittest discover -s tests -v
python tools\capture_screen.py --list-monitors
```

完成新修改后先检查差异，再按项目提交规范提交；已有本地提交可用 `git push` 同步。再次移动或更名文件夹时，应同步修改 Codex 项目的关联路径，并让命令使用新的工作目录，无需重新初始化 Git。

本次只调整文档和仓库连接。官方 Computer Use 原生截图仍未恢复，已安装辅助程序保持此前回退后的原始版本。
